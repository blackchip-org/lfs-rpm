%dist .lfs12

%_build_id_links none

%lfs        /lfs
%tools      /lfs/tools
%lfs_tgt    x86_64-lfs-linux-gnu

%lfs_path export PATH=%{tools}/bin:${PATH}
%lfs_remove_docs rm -rf %{buildroot}/%{lfs}/usr/share/{doc,info,man}
